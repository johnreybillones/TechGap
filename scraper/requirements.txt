# TechGap Scraper Dependencies
apify-client>=1.7.0        # Apify Python SDK — runs shahidirfan/jobstreet-scraper actor
pydantic>=2.0.0            # Data validation and schema enforcement
pydantic-settings>=2.0.0   # .env config loading via ScraperConfig
python-dotenv>=1.0.0       # .env file support
supabase>=2.0.0            # Supabase Python client for jobs_raw upsert
httpx[http2]>=0.27.0       # Required by supabase SDK internally
spacy>=3.7.0               # NLP for seniority/skill extraction (Week 2 — not yet active)
